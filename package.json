{
  "name": "test",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wuzi",
  "license": "MIT",
  "description": "A library that process a set of historical patient data and compute a score for each patient",
  "devDependencies": {
    "@types/node": "^22.3.0",
    "typescript": "^5.5.4"
  }
}
