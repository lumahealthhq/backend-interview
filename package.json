{
  "name": "luma",
  "version": "1.0.0",
  "description": "generates a call list for wait list patients",
  "scripts": {
    "server-dev": "nodemon ./index.js",
    "test": "mocha testSpec.js --exit"
  },
  "author": "vanessayip",
  "dependencies": {
    "bluebird": "^3.5.1",
    "chai": "^4.1.2",
    "dotenv": "^5.0.0",
    "mocha": "^5.0.1"
  },
  "devDependencies": {
    "supertest": "^3.0.0"
  }
}
