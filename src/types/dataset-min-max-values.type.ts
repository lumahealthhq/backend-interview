export type DatasetMinMaxValues = {
  age: {min: number; max: number};
  distanceToFacility: {min: number; max: number};
  acceptedOffers: {min: number; max: number};
  canceledOffers: {min: number; max: number};
  averageReplyTime: {min: number; max: number};
};
